# Cursor ignore file
# This file controls which files Cursor should ignore

# Ignore all .pem files by default for security
*.pem

# But allow .pem files in test resources directories
!**/tests/resources/*.pem
!**/test/resources/*.pem
!**/test-resources/*.pem

# Ignore other sensitive files
*.key
*.p12
*.pfx
*.crt
*.cer
*.der
*.p7b
*.p7c

# Allow test certificates and keys in test directories
!**/tests/**/*.key
!**/tests/**/*.crt
!**/tests/**/*.cer
!**/test/**/*.key
!**/test/**/*.crt
!**/test/**/*.cer
